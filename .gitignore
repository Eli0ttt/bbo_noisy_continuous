# Secrets / local credentials
env/dsh_bbo_experiment.env
.env
.env.*
!.env.example
!*.env.example

# Runtime / derived experiment outputs
dsh_rsi_jobs/
results/
runs/
*.agent-trace.tar.zst
*.agent-trace.manifest.txt
*.protocol.txt

# Local derived task staging. Keep it on the experiment machine because the
# current formal-run.sh uses it, but do not duplicate it in the custom-code repo.
dsh_rsi_runs/

# Local checkout of the official RSI-Exam infrastructure. Keep it locally for
# formal-run.sh; track it later as a real Git submodule if desired.
official_rsi/

# Python/editor caches
__pycache__/
**/__pycache__/
*.py[cod]
.DS_Store

# Local scratch/backups
*.tmp
*.bak
*~

# Local installation/image bundles
packages/
